#include <stdio.h>
#include "holberton.h"
/**
 *main - prints the number of arguments
 *@argc: argument count
 *@argv: argument vector
 *Return: zero
 */
int main (int argc, char *argv[])
{
	printf("%d\n", argc - 1);
	argv = argv;

	return (0);
}
